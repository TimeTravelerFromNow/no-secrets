<%= form_with(model: hacker) do |form| %>
  <% if hacker.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(hacker.errors.count, "error") %> prohibited this hacker from being saved:</h2>

      <ul>
        <% hacker.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

    <div>
      <%= form.label :portrait, style: "display: block" %>
      <%= form.file_field :portrait %>
    </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
